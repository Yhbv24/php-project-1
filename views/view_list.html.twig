<!DOCTYPE html>
<html>
<head>
  <link href="css/bootstrap.min.css" rel="stylesheet" type="text/css">
  <link href="css/styles.css" rel="stylesheet" type="text/css">
  <link href="https://fonts.googleapis.com/css?family=Dosis" rel="stylesheet">
  <title>Contact List</title>
</head>
<body>
  <h1 class="jumbotron">Your Contact List</h1>
  <div class="container">
    <h4>Here, you'll find a list of all of the contacts you've added. Feel free to add a contact when you make a new friend.</h4>
    <a href="/"><button type="button" class="btn">Go Home</button></a>
    <div class="row">
      <div class="col-sm-12">
        {% if all_contacts is not empty %}
        {% for contact in all_contacts %}
        <ul>
          <li class="main_contact_name"><strong>{{ contact.getFullName }}</strong></li>
            <ul>
              <li>{{ contact.getPhoneNumber }}</li>
              <li>{{ contact.getStreet }}</li>
              <li>{{ contact.getCity}}, {{ contact.getState}}, {{ contact.getZip}}</li>
            </ul>
        </ul>
        {% endfor %}
        {% endif %}

        {% if all_contacts is empty %}
        <p>You don't have any contacts at the moment!</p>
        {% endif %}
      </div>
    </div>
  </div>
</body>
</html>
